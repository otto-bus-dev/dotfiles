# Personal user info is in .gitconfig.local (not tracked in git)
# Create ~/.gitconfig.local with:
#   [user]
#       email = your.email@example.com
#       name = Your Name
[include]
	path = ~/.gitconfig.local
